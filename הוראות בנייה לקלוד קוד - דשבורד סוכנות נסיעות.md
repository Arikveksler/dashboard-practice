# הוראות בנייה — דשבורד "מד מהירות" לסוכנות נסיעות

## התוכן בקצרה

בנה אפליקציית דשבורד web (React + Tailwind, אפשר Vite) שמציגה נתוני סוכנות נסיעות בעיצוב של **לוח מחוונים של רכב ספורט** — שעונים אנלוגיים עגולים (כמו מד מהירות / מד סל"ד), בפלטת **אדום ושחור**. כל מדד מוצג פעמיים: כשעון גרפי (Gauge) **וגם** כמספר גדול רשום מתחתיו עם כותרת שמסבירה מה המדד מציג. מתחת לכל השעונים — טבלת נתונים מלאה וברורה.

קבצי הנתונים מצורפים (ראה "קבצי קלט" בהמשך). זהו דאטה דמה לצורך תרגול — אין להמציא נתונים נוספים, יש להשתמש רק בקבצים המצורפים ולחשב מהם את כל המדדים.

---

## קבצי קלט (מצורפים)

1. **`bookings.csv` / `bookings.json`** — 720 שורות הזמנות גולמיות. עמודות:
   - `id` — מזהה הזמנה (למשל TRV-10015)
   - `date` — תאריך ההזמנה (YYYY-MM-DD, טווח אוקטובר 2025–ספטמבר 2026)
   - `city`, `country` — יעד הנסיעה
   - `pax` — מספר נוסעים
   - `type` — סוג הזמנה: חבילה / טיסה בלבד / מלון בלבד / קרוז / טיול מודרך
   - `agent_id` — מספר סוכן (4 ספרות, 6 סוכנים קבועים בדאטה)
   - `channel` — מקור ליד/ערוץ מכירה: אתר / טלפון / וואטסאפ / סוכן משנה / מטא Ads
   - `status` — שולם / ממתין לתשלום / בוטל
   - `revenue` — הכנסה בש"ח
   - `profit` — רווח בש"ח
   - `nights` — מספר לילות

2. **`agent_performance.csv` / `agent_performance.json`** — טבלה מסוכמת לפי סוכן (6 שורות, אחת לכל `agent_id`):
   - `agent_id`, `total_bookings`, `paid_bookings`, `cancelled_bookings`, `total_pax`, `unique_destinations`, `total_revenue`, `total_profit`

טען את שני הקבצים לאפליקציה (כ-JSON סטטי בצד ה-client מספיק — אין צורך בבקאנד/DB לתרגיל הזה, אלא אם תרצה להוסיף בעצמך).

---

## עיצוב — "לוח מחוונים של רכב ספורט"

### פלטת צבעים
- רקע: שחור/אפור פחם עמוק (`#0a0a0a` עד `#1a1a1a`), עם טקסטורת "פנל" עדינה (גרדיאנט רדיאלי כהה או noise קל) — לא צבע אחיד שטוח
- מסגרות שעונים: שחור מט עם הבהוב מתכתי דק (`#2a2a2a`–`#3a3a3a`) לדמות טבעת כרום/פלסטיק
- מחוג ואזור "אדום גבוה" (redline): אדום דהרבי/פרארי (`#e10600` עד `#ff1e1e`), עם glow/blur עדינה בצבע אדום מסביב למחוג ולערכים החמים
- טקסט מספרים: לבן/כסף בוהק (`#f5f5f5`), עם accent אדום לכותרות ולערכים קריטיים
- קווי סקאלה על השעון: אפור בהיר בגוונים קרים, עם קטע אדום ב-20%-25% העליונים של הסקאלה (redline)
- טיפוגרפיה: פונט sans-serif טכני/דיגיטלי למספרים הגדולים (למשל Rajdhani, Orbitron, Space Grotesk, או Digital-7 לתחושת דיגיטלי-אנלוגי) — טקסט תוויות בפונט sans רגיל וקריא

### רכיב "שעון" (Gauge) — אחיד לכל המדדים
כל שעון בונה מ:
1. **חוג עגול (arc gauge)** בין 0 למקסימום הגיוני של המדד (ראה מיפוי מדדים למטה) — SVG או canvas, מחוג אדום שמסתובב לפי הערך, עם קפיצה/אנימציה חלקה כשהערך נטען (ease-out, לא snap מיידי)
2. **מספר גדול** מתחת לשעון — הערך המדויק בפועל (לא מעוגל אם לא צריך), עם יחידה (₪, הזמנות, %, וכו')
3. **כותרת** מעל/מתחת למספר — משפט קצר שמסביר בדיוק מה השעון מציג (עד 5-6 מילים), למשל: "סה״כ הכנסה מכל ההזמנות" או "הזמנות ליעד: פריז"
4. אופציונלי: תג delta קטן (חץ + %) אם יש בסיס להשוואה (למשל לעומת החודש הקודם)

בנה קומפוננטת `<GaugeCard value={} max={} label={} sublabel={} unit={} />` אחת לשימוש חוזר לכל השעונים.

---

## מבנה הדשבורד ותוכן השעונים

### 1. סקשן עליון — כל הנתונים (מבט כללי)
שעונים על בסיס `bookings`, מחושבים מכל הדאטה:
- **סה"כ הזמנות** (ספירת שורות)
- **סה"כ הכנסה** (סכום `revenue` לשורות עם status = שולם)
- **סה"כ רווח** (סכום `profit` לשורות עם status = שולם)
- **סה"כ נוסעים** (סכום `pax`)
- **אחוז הזמנות ששולמו** (paid / total * 100)
- **אחוז ביטולים** (cancelled / total * 100) — שים לב: זה מדד "שלילי", שים אותו בקטע האדום של הסקאלה גם בערכים נמוכים-יחסית כדי להדגיש חשיבות
- **ממוצע לילות להזמנה**

### 2. סקשן — יעדים (Destinations)
- **בורר יעד** (dropdown/רשימת כרטיסים לבחירה) מתוך רשימת הערים ב-`city`: פריז, רומא, ברצלונה, לונדון, אתונה, דובאי, בנגקוק, ניו יורק, לרנקה, פראג, אמסטרדם, טביליסי
- לאחר בחירת יעד, הצג שעונים המחושבים רק על שורות אותו יעד:
  - **מספר הזמנות ליעד הנבחר**
  - **הכנסה מהיעד הנבחר**
  - **ממוצע נוסעים להזמנה ביעד**
  - **יעד עם הכי הרבה הזמנות** (חשב ותציג את השם + המספר — אפשר כשעון "מיוחד" קבוע שלא תלוי בבחירה, לצד הבורר)
- אפשר גם גרף עזר (bar chart קטן, בגווני אדום/שחור) שמציג את כל 12 היעדים לפי מספר הזמנות, לצד השעונים

### 3. סקשן — מקורות לידים (Channels)
שעונים/גרף לפי `channel` (אתר / טלפון / וואטסאפ / סוכן משנה / מטא Ads):
- **מספר הזמנות לכל ערוץ** (שעון או פס לכל ערוץ — 5 ערכים)
- **ערוץ מוביל** — הערוץ עם הכי הרבה הזמנות, כשעון מודגש
- **הכנסה לפי ערוץ** — לאיזה ערוץ יש את ההכנסה הגבוהה ביותר
- אפשר להציג את 5 הערוצים כ-5 שעונים קטנים בשורה אחת, או כשעון אחד + donut/bar chart תחתיו

### 4. סקשן — ביצועי סוכנים (מהקובץ `agent_performance`)
שעונים נפרדים לכל סוכן (6 סוכנים) או שעון אחד עם בורר סוכן (dropdown לפי `agent_id`), מציג:
- **סה"כ הזמנות של הסוכן** (`total_bookings`)
- **הזמנות ששולמו** (`paid_bookings`)
- **הזמנות שבוטלו** (`cancelled_bookings`) — קטע אדום
- **יעדים ייחודיים ששירת** (`unique_destinations`)
- **הכנסה כוללת** (`total_revenue`)
- **רווח כולל** (`total_profit`)
- הוסף גם **טבלת דירוג (leaderboard)** קטנה של 6 הסוכנים ממוינים לפי `total_revenue` — עם מדליה/מספר דירוג ליד המוביל, עדיין בסטייל שחור-אדום

---

## טבלאות נתונים (מתחת לכל התצוגה הגרפית)

בתחתית העמוד, מתחת לכל השעונים והגרפים, הוסף **שתי טבלאות מלאות**:

1. **טבלת כל ההזמנות** — כל 720 השורות מ-`bookings`, עם כל העמודות (id, date, city, country, pax, type, agent_id, channel, status, revenue, profit, nights). כולל: מיון לפי עמודה (קליק על כותרת), חיפוש/סינון חופשי, pagination (25-50 שורות בעמוד), וצביעת סטטוס (שולם=ירוק/אפור בהיר, ממתין=כתום, בוטל=אדום)
2. **טבלת ביצועי סוכנים** — 6 השורות מ-`agent_performance`, עם כל העמודות, ממוינת כברירת מחדל לפי `total_revenue` בסדר יורד

עיצוב הטבלאות: כהה (רקע שחור/אפור פחם), טקסט לבן, כותרות עמודות עם underline אדום דק, שורה מודגשת בהובר בגוון אדום-שקוף.

---

## דרישות טכניות
- React + Vite + Tailwind CSS (או כל סטאק דומה שקלוד קוד מעדיף)
- קומפוננטת Gauge אחת לשימוש חוזר (SVG מחוג + arc), לא לבנות שעון נפרד לכל מדד
- responsive: בדסקטופ שעונים בגריד (3-4 בשורה), במובייל גריד של 1-2 בשורה
- כל הנתונים מחושבים מקבצי ה-CSV/JSON המצורפים בזמן טעינה (client-side) — לא לכתוב מספרים קבועים בקוד
- אנימציית "עלייה" של המחוגים בטעינת הדף/שינוי בורר (לא קפיצה מיידית)
- RTL מלא — כל הטקסטים בעברית, כיווניות מימין לשמאל

---

## קבצים מצורפים לתרגיל
- `bookings.csv`
- `bookings.json`
- `agent_performance.csv`
- `agent_performance.json`
